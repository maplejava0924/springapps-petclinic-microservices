spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/maplejava0924/springapps-petclinic-microservices-config
        native:
          search-locations: classpath:.
  profiles:
    active: native
